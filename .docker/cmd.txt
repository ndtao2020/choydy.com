Setting an Environment Variable

- export POSTGRES_DB=postgres
- export POSTGRES_USER=postgres
- export POSTGRES_PASSWORD=1234567890

- export REDIS_PASSWORD=1234567890

- export QUARKUS_KEYSTORE_ALIAS=it
- export QUARKUS_KEYSTORE_PATH=1234567890
- export QUARKUS_KEYSTORE_PASS=./keystore.jks

- export

First, ufw needs to be enabled. From a terminal prompt enter:

- sudo ufw enable
- sudo ufw allow http
- sudo ufw allow https
- sudo ufw status
- sudo ufw status verbose
- sudo ufw allow from 203.0.113.4 to any port 22

sudo ufw allow 'Apache Full'
sudo ufw delete allow 'Apache'

To open a port (SSH in this example):

- sudo ufw allow 22

Rules can also be added using a numbered format:

- sudo ufw insert 1 allow 80

Similarly, to close an opened port:

- sudo ufw deny 22

To remove a rule, use delete followed by the rule:

- sudo ufw delete deny 22 && sudo ufw delete deny 22

It is also possible to allow access from specific hosts or networks to a port. The following example allows SSH access from host 192.168.0.2 to any IP address on this host:

- sudo ufw allow proto tcp from 192.168.0.2 to any port 22

sudo ufw default deny incoming
sudo ufw default allow outgoing

sudo ufw allow 5432/tcp
sudo ufw allow 6379/tcp

sudo ufw deny 5432/tcp && sudo ufw delete deny 5432/tcp
sudo ufw deny 6379/tcp && sudo ufw delete deny 6379/tcp

sudo ufw reload

#######################################################################################################################

sudo reboot

# change password
sudo passwd root

# update
sudo apt update && sudo apt upgrade -y
sudo apt autoremove
sudo apt list --upgradable
sudo apt clean && sudo apt autoclean && sudo apt autoremove

sudo apt-get install nginx
sudo apt-get install certbot
apt-get install python3-certbot-nginx
sudo certbot --nginx -d choydy.com -d www.choydy.com
sudo certbot --nginx -d file.choydy.com
crontab -e
crontab -l
0 12 * * * /usr/bin/certbot renew --quiet

nginx -t && nginx -s reload

sudo systemctl restart nginx
sudo systemctl reload nginx

sudo certbot --nginx -d choydy.com/ -d www.choydy.com

sudo chmod -R 777 /home/sixven/camp_sms/inputs

/home/choydy/ssl

chmod +x init-letsencrypt.sh
sudo ./init-letsencrypt.sh

cd /home/choydy && docker system prune --force && docker pull taoqn/choydy-com:latest && docker-compose up -d --build --remove-orphans
